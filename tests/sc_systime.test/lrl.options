round_robin_stripes
enable_snapshot_isolation
setattr MAX_SHADOW_ROWS 10000000
# disable these until a snapshot bug is fixed
sql_release_locks_on_si_lockwait off
sql_release_locks_in_update_shadows off
